# Project journal — 2026-08

This file is append-only. Corrections must be added as new dated entries.

## 2026-08-02 — Foundation specification and implementation candidate

- Goal: establish junet.store as a bilingual, mobile-first AI/MQL5/open-source
  knowledge hub with MQL5 CodeGraph as the flagship project.
- Governance: constitution `1.0.0`; feature `001-junet-store-foundation`; ADRs
  0001–0005 accepted.
- Changes: initialized the Sites Vinext runtime; implemented stable locale and
  article identities, system-first persistent theme, responsive design, strict
  local Markdown, five bilingual articles, flagship/community surfaces,
  canonical/hreflang/sitemap/robots, localized 404s, response security policy,
  CI configuration, architecture docs, runbooks, changelog, and handoff.
- Verification: TypeScript, ESLint, Vinext production build, 3 content tests,
  4 locale/theme/UI contract tests, and 7 built-Worker response tests passed.
  The rendered gate covers 22 public routes, root negotiation, exact translation
  metadata, security headers, sitemap/robots, flagship links, and localized 404s.
- Dependency evidence: Next upgraded from 16.2.6 to 16.2.12; production PostCSS
  and Sharp overridden to patched releases; `npm audit --omit=dev` reports zero.
- Visual: reviewed official MQL5 CodeGraph hero added; one bespoke generated
  social card saved as `public/og.png`, with exact `junet.store` and
  `AI × MQL5 × Open Source` copy verified visually.
- Graphify/GitNexus: pending until the implementation/docs baseline is committed.
- Release: private Sites version, GitHub commit/deploy SHA, browser matrix,
  Lighthouse, DNS cutover, and production approval are pending.
- Risks: Vinext remains pre-1.0; current RSC output needs `unsafe-inline` for
  scripts/styles but never `unsafe-eval`; mobile/browser and deployed CSP must be
  proven on the private candidate; no initial static-GET rate rule is recommended
  without observed abuse.
- Next: run full local gate, visual browser matrix, Graphify and GitNexus; create
  public GitHub source history; deploy exact SHA privately; request approval.

## 2026-08-02 — Mobile and performance release evidence

- Goal: prove responsive behavior and performance against the V1 budgets before
  generating code-intelligence and deployment evidence.
- Browser evidence: Chromium at `412×915`, `768×1024`, and `1440×900`; WebKit
  with iPhone SE and iPhone 15 Pro profiles. Verified zero horizontal overflow,
  localized mobile menu, body scroll lock, exact article identity switching,
  theme persistence through reload, install-command copy interaction, and clean
  console after CSP correction. Screenshots/traces remain ignored under
  `output/playwright/`.
- Fixes from live QA: changed favicon metadata to a same-origin relative link;
  removed CSP `upgrade-insecure-requests` because WebKit correctly upgraded
  localhost assets to unavailable HTTPS; production HTTPS remains an edge
  redirect. Optimized the reviewed flagship hero from 525,462-byte PNG to a
  26,946-byte 960-pixel WebP.
- Lighthouse production Worker evidence: `/vi` scores Performance 97,
  Accessibility 100, Best Practices 100, SEO 100; LCP 2,106 ms and CLS 0.
  `/en/mql5-codegraph` scores 97/100/100/100; LCP 2,221 ms and CLS 0.
- Tool caveat: Lighthouse wrote valid JSON reports, then returned exit 1 because
  its Windows launcher could not delete a locked temporary profile (`EPERM`).
  Scores above were read from the completed reports, not inferred from the exit.
- Operations: Vinext `start` did not serve bundled assets correctly in this local
  build, so the production-compatible Worker was audited through Wrangler using
  `dist/server/wrangler.json` and its `dist/client` asset binding. Wrangler must
  be stopped before rebuilding on Windows because it locks `dist/server/.wrangler`.
- Risk: repeat Lighthouse and security-header checks on the private Sites URL;
  local edge emulation is strong evidence but not production truth.
- Next: commit this validated baseline, run directed deep Graphify and GitNexus,
  then create the public repository and private Sites candidate.

## 2026-08-02 — Code-intelligence baseline

- Goal: establish durable architecture evidence after commit `b8d7937` and
  assess the feature branch blast radius before remote review.
- Graphify: directed deep extraction over 113 files and approximately 117,937
  words produced 315 nodes, 425 directed edges, and 31 communities. Confidence
  is 72% `EXTRACTED`, 28% `INFERRED`, 0% `AMBIGUOUS`; token usage is recorded as
  zero measured because this collaboration host did not expose sub-agent usage.
- Graphify health: 0 missing/dangling endpoints, self-loops, exact duplicates,
  or directed endpoint collapses after reconciling a Windows AST path-prefix and
  missing file-node issue. Ten pairs collapse only in an undirected projection;
  their reverse directions remain intentionally distinct in the `DiGraph`.
- Graphify outputs: reviewed `graph.json`, `GRAPH_REPORT.md`, and `graph.html`.
  God nodes include MQL5 CodeGraph, the prioritized feature-spec template,
  Locale, and the Junet.store Constitution. No import cycle was detected.
- GitNexus: canonical root `F:/Dev/Projects/junet-store`, indexed commit
  `b8d7937` as `junet-store`; 470 nodes, 643 edges, 13 clusters, and 10 flows.
  Full branch comparison is High because it introduces 107 files/327 symbols
  and affects all 10 new flows. Scoped upstream impact is Low for
  `alternatePath` (1 test consumer), `assertArticleParity` (5 consumers through
  content/page layers), and `ThemeToggle` (2 consumers; LocaleLayout flow).
- Interpretation: initial-feature aggregate risk is addressed by the green full
  gate, complete bilingual route render tests, browser matrix, and private-only
  deployment policy. No unresolved scoped High impact was found.
- Risk: Graphify has 59 weakly connected nodes and several low-cohesion broad
  communities; treat the graph as navigation evidence, not proof of runtime.
- Next: commit reviewed graph outputs and journal, publish the feature branch,
  require remote CI, then create an exact-SHA private Sites preview.

## 2026-08-02 — Remote governance and dependency-gate correction

- Goal: prove the repository controls and make the audit gate cover the entire
  dependency graph instead of production packages only.
- GitHub: created public repository `junet03/junet-store` with a draft V1 pull
  request. `main` requires pull requests, strict up-to-date Quality checks on
  Ubuntu and Windows, CodeQL, Dependency Review, resolved conversations, and
  linear history; admin enforcement is enabled and force-push/delete are off.
  Issues, Discussions, Dependency Graph/alerts, Dependabot security updates,
  secret scanning, and push protection are enabled.
- CI evidence: both OS validation jobs, JavaScript/TypeScript CodeQL, and
  Dependency Review passed. The first Dependency Review attempt failed only
  because the new repository's Dependency Graph had not yet been enabled; it
  passed after that repository setting was corrected without changing source.
- Audit correction: the earlier production-only audit was insufficient for the
  stated gate. A full `npm audit` exposed High findings in build dependencies;
  React/RSC, Vite, Wrangler, Cloudflare Vite, and affected transitive packages
  were upgraded to patched compatible releases. Fresh `npm ci`, the complete
  source/build/rendered gate, and `npm audit --audit-level=high` now pass with
  zero vulnerabilities and no exception.
- Sites: private version 1 from commit `823a6e8` deployed successfully at the
  owner-only preview URL. It is a provisional rollback point, not the release
  candidate, because the complete dependency audit hardening happened afterward.
- GitNexus refresh: indexed the security-hardened code commit `6ea1049` with
  505 nodes, 677 edges, 13 clusters, and 10 flows. Whole-feature comparison
  remains High because V1 is new; scoped upstream impact is Low with no callers
  for `localBindingConfig` and no upstream consumers for `LocalizedPage`.
  GitNexus did not resolve the imported `sites` helper as a symbol, so the clean
  typecheck, production build, and rendered Worker tests remain the governing
  evidence for that import-path-only change.
- Risk: the in-app browser webview did not attach to the owner-only preview in
  time; final hosted smoke evidence must therefore use Sites-authorized HTTP
  checks and the security-hardened saved version.
- Next: commit and push the dependency correction, re-run remote gates, save and
  privately deploy the exact corrected SHA, smoke it, then request production
  approval without changing DNS or public access.

## 2026-08-02 — Private release candidate accepted by automated gates

- Goal: produce the exact private V1 candidate and stop at the maintainer
  production-approval boundary.
- Candidate: Sites version 2 was saved from pushed commit `232d340` and deployed
  successfully to `https://junet-store.demir1852refija.chatgpt.site`. Access is
  custom owner-only: one allowed user, no editors, no groups, and no external
  visitors. Version 1 at commit `823a6e8` remains the immediate rollback target.
- Hosted smoke: root returned `307` to `/en` for an English `Accept-Language`;
  `/vi`, `/en`, and both flagship pages returned localized `200` HTML; invalid
  Vietnamese and English article paths returned `404`. Every sampled response
  carried CSP with `frame-ancestors 'none'`, `object-src 'none'`, and
  `form-action 'none'`, plus `X-Content-Type-Options: nosniff` and frame denial.
  Flagship and Telegram destinations were present in both locales.
- Remote gate: draft PR 1 at exact head `232d340` is `CLEAN`. Ubuntu and Windows
  validation, JavaScript/TypeScript CodeQL, Dependency Review, and the CodeQL
  summary all passed. Protected `main` requires the four durable job contexts.
- Visual evidence: the earlier Chromium/WebKit matrix and Lighthouse evidence
  remain applicable because dependency hardening did not change rendered UI.
  The owner-only in-app webview did not attach, so hosted proof is HTTP-level;
  this limitation is explicit and was not represented as visual production QA.
- Risk/decision: public production, custom domain, DNS, Cloudflare WAF/rate
  limits, and HSTS remain untouched. They require explicit approval, current DNS
  backup, TLS proof, and a post-cutover browser/security smoke pass.
- Next: maintainer reviews the owner-only Sites URL and draft PR, then either
  requests changes or explicitly approves the production cutover checkpoint.

## 2026-08-02 — Production V1 approved and released

- Goal: execute the maintainer-approved production cutover, preserve a tested
  rollback path, and close the release only after live custom-domain evidence.
- GitHub release chain: PR 1 merged through protected `main` at `d8b7cb8`;
  production edge PR 7 merged at `32734db`; static-delivery correction PR 8
  merged at `f5e6e7f`; final source-owned dynamic-asset PR 9 merged at
  `0c263b41be0c5e67516b019dc164daf15b7ffc57`. Every PR passed Ubuntu and
  Windows validation, JavaScript/TypeScript CodeQL, and Dependency Review.
- Sites: public version 3 established the custom-domain cutover. Versions 4 and
  5 activated HSTS/cache policy and exposed a platform-specific static-asset
  bypass during live smoke. Final production version 6
  (`appgprj_6a6e4e5c613081918a0b7004b0e5e9da~appgver_bd3b0bcee3e4819181cd4b80c249e6f6`)
  was saved from exact commit
  `0c263b41be0c5e67516b019dc164daf15b7ffc57` and deployment
  `appgdep_6a6ed1bfa7008191adf32ecbf8c4d570`
  succeeded. Version 5
  (`appgprj_6a6e4e5c613081918a0b7004b0e5e9da~appgver_4b1ccef17d788191a6a9075f84084c4f`)
  remains the immediate Sites rollback artifact.
- DNS and TLS: the pre-cutover apex `198.54.119.178` is the rollback target.
  Apex now uses Sites targets `162.159.143.30` and `172.66.3.26`; the Sites and
  Cloudflare validation TXT records are present. Existing `www`, MX, SPF, DKIM,
  DMARC, cPanel, and tunnel records were preserved. Sites reports the custom
  domain, provider, and SSL as active.
- Production smoke: `https://junet.store/vi`, `/en`, and the flagship route
  return localized `200`; localized missing content returns `404`; root locale
  negotiation is `307` with `private, no-store`; HTTP redirects to HTTPS and
  `www` redirects to apex. HTML carries the strict CSP, nosniff, frame denial,
  HSTS `max-age=2592000`, and one-hour edge cache policy.
- Static delivery evidence: Sites serves physical static files ahead of the
  Worker despite the uploaded `run_worker_first` setting. The final application
  therefore emits virtual `/edge-assets/` and `/edge-media/` URLs, validates
  their paths, proxies them through the asset binding, and owns their response
  policy in source. Live WebP is `image/webp` with a one-day cache; hashed CSS
  is `text/css` with `public, max-age=31536000, immutable`; both carry HSTS.
- Cloudflare live truth: the zone is Free with automatic DDoS protection.
  Managed WAF rules require Pro, and the single available rate-limit rule offers
  `Block` rather than the planned Managed Challenge. No unobserved Block rule
  was deployed. Cache/response-transform experiments that did not govern the
  Sites custom hostname were removed rather than retained as misleading policy.
- Code intelligence: final GitNexus index at code commit `7e53939` contains
  542 nodes, 723 edges, 13 clusters, and 10 flows. The four-file dynamic-asset
  change maps to 18 symbols, zero affected processes, and Low risk; upstream
  impact for `rewriteAssetPaths` is one direct Worker consumer. Directed
  Graphify at `7e53939` contains 500 nodes, 712 edges, and 22 communities with
  zero missing/dangling endpoints, self-loops, or exact duplicate edges.
- Risks and next step: HSTS intentionally excludes subdomains and preload during
  the first observation window. Review Cloudflare Security Events and production
  health daily for seven days; document evidence before choosing a Block rule or
  a plan upgrade. Roll back by redeploying Sites version 5 and, only if needed,
  restoring the recorded apex value through the deployment runbook.
